import 'fake-indexeddb/auto';
import { beforeEach, afterEach } from 'vitest';
import { cleanup } from '@testing-library/react';

// Reset mock DOM before and after each test
beforeEach(() => {
  // Setup standard mocks
});

afterEach(() => {
  cleanup();
});
